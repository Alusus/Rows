import "Receive";
import "DbConBuilder";

def table: Receive;
def Data: Array[Array[String]];
def t: Array[String];
t.add(String("cars"));
table.setTableName(t);

table.setExecString();
def ex: String;
ex = table.getExecString();
Console.print(ex);
def DbCon:  DbConBuilder;
DbCon.setDBName(String("testdb"));
DbCon.setUserName(String("user12"));
DbCon.setPassword(String("Qwerty231@#!"));
DbCon.setServer(String("localhost"));
def mysql2: MySql.Db;
mysql2 = DbCon.mySqlConBuilder();
table.setMySqlObj(mysql2);
table.setData();
Data = table.getData();
def i: int = 0;
def j: int = 0;
for i = 0, i < Data.getLength(), i = i + 1 {
    for j = 0, j < Data(i).getLength(), j = j + 1 {
        Console.print(Data(i)(j));
        Console.print("    ");
    }
    Console.print("\n");
}

